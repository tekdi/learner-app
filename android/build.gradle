buildscript {
    ext {
        buildToolsVersion = "35.0.0"
        minSdkVersion = 28  // Updated to 28 for Zoom SDK 6.6.0 compatibility
        compileSdkVersion = 35
        targetSdkVersion = 35
        ndkVersion = "26.1.10909125"
        kotlinVersion = "1.9.22"
    }
    repositories {
        google()
        mavenCentral()
        // Zoom SDK Maven Repository
        maven {
            url 'https://github.com/zoom/zoom-sdk-android/raw/master/mobilertc-android-studio/release'
        }
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.6.0")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
            classpath 'com.google.gms:google-services:4.4.2'

    }
}

//for background task sync
//react-native-background-fetch
allprojects {
    repositories {
        google()
        mavenCentral()
        mavenLocal()
        maven {
            // All of React Native (JS, Obj-C sources, Android binaries) is installed from npm
            url("$rootDir/../node_modules/react-native/android")
        }
        maven {
            // Android JSC is installed from npm
            url("$rootDir/../node_modules/jsc-android/dist")
        }
        maven {
          // react-native-background-fetch
          url("${project(':react-native-background-fetch').projectDir}/libs")
        }
        // Zoom SDK Maven Repository
        maven {
            url 'https://github.com/zoom/zoom-sdk-android/raw/master/mobilertc-android-studio/release'
        }
    }
}
//end for background task sync
//end react-native-background-fetch

apply plugin: "com.facebook.react.rootproject"
